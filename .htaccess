# InfinityFree-compatible .htaccess for SPA/static site # 1. Disable MultiViews to prevent conflicts
Options -MultiViews # 2. Set the default file DirectoryIndex index.html

<IfModule mod_rewrite.c>
  RewriteEngine On RewriteBase / # 3. Force HTTPS RewriteCond %{HTTPS} off RewriteRule ^(.*)$
  https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301] # 4. SPA Routing (The Fix) # If the file/folder
  doesn't exist, route everything to index.html RewriteCond %{REQUEST_FILENAME} !-f RewriteCond
  %{REQUEST_FILENAME} !-d RewriteRule ^ index.html [QSA,L]
</IfModule>
